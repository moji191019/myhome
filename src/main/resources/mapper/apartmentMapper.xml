<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moji.home.dao.ApartmentDAO">

    <select id="findApartment" resultType="com.moji.home.dto.ApartmentDTO">
        <!-- SELECT dorojuso, name AS apt_nm, adres_city, adres_gu, adres_dong, adres_doro FROM Product_Address WHERE adres_dong = #{keyword}; -->
        SELECT dorojuso, name AS apt_nm, adres_city, adres_gu, adres_dong, adres_doro 
        FROM Product_Address 
        WHERE adres_gu LIKE '%' #{keyword} '%' OR
        adres_dong LIKE '%' #{keyword} '%' OR
        adres_doro LIKE '%' #{keyword} '%' OR
        name LIKE '%' #{keyword} '%'
    </select>
    <!--
    <select id="findMember" parameterType = "String" resultType="com.moji.home.dto.ApartmentDTO">
        SELECT * FROM test2 WHERE key1 = #{key1}
    </select>

    <insert id="insertMember" parameterType="com.moji.home.dto.ApartmentDTO">
        INSERT INTO test2 VALUES (#{key1}, #{key2}, #{key3}, #{key4})
    </insert>
    -->
</mapper>